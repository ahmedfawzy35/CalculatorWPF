<UserControl x:Class="CalculatorWPF.Views.StandardCalculatorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="500"
             Focusable="True"
             KeyDown="UserControl_KeyDown">

    <UserControl.Resources>
        <!-- Windows 11 Style Calculator Button Base -->
        <Style x:Key="Win11CalcButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SurfaceContainerHighBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="MinHeight" Value="56"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource SurfaceContainerHighestBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource PrimaryContainerBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Function Operator Button Style -->
        <Style x:Key="Win11OpButtonStyle" TargetType="Button" BasedOn="{StaticResource Win11CalcButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource SurfaceContainerHighestBrush}"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <!-- Primary Equals Accent Button Style -->
        <Style x:Key="Win11EqualButtonStyle" TargetType="Button" BasedOn="{StaticResource Win11CalcButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.75"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Compact Memory Button Style -->
        <Style x:Key="Win11MemoryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource SurfaceContainerHighBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource OutlineBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="Transparent" Margin="8" Width="520" MaxHeight="680" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Expression & Output Display Screen -->
            <RowDefinition Height="Auto"/> <!-- Memory Bar -->
            <RowDefinition Height="*"/>    <!-- Keypad Grid -->
        </Grid.RowDefinitions>

        <!-- 1. Display Screen (Windows 11 Fluent style display) -->
        <Border Grid.Row="0"
                Background="{DynamicResource SurfaceContainerBrush}"
                CornerRadius="14"
                Padding="24,18"
                Margin="0,0,0,12"
                BorderBrush="{DynamicResource CardBorderBrush}"
                BorderThickness="1">
            <Grid FlowDirection="LeftToRight" HorizontalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <!-- Previous Expression Line -->
                <TextBlock Grid.Row="0"
                           Text="{Binding ExpressionText}"
                           FontSize="16"
                           FontWeight="Medium"
                           Foreground="{DynamicResource TextMutedBrush}"
                           HorizontalAlignment="Right"
                           MinHeight="26"
                           TextTrimming="CharacterEllipsis"/>

                <!-- Active Primary Display Result -->
                <TextBlock Grid.Row="1"
                           Text="{Binding DisplayText}"
                           FontSize="52"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           HorizontalAlignment="Right"
                           Margin="0,4,0,0"
                           TextTrimming="CharacterEllipsis"/>
            </Grid>
        </Border>

        <!-- 2. Memory Buttons Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,10" FlowDirection="LeftToRight">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="MC" Command="{Binding MemoryClearCommand}" IsEnabled="{Binding IsMemoryActive}" Style="{StaticResource Win11MemoryButtonStyle}"/>
            <Button Grid.Column="1" Content="MR" Command="{Binding MemoryRecallCommand}" IsEnabled="{Binding IsMemoryActive}" Style="{StaticResource Win11MemoryButtonStyle}"/>
            <Button Grid.Column="2" Content="M+" Command="{Binding MemoryAddCommand}" Style="{StaticResource Win11MemoryButtonStyle}"/>
            <Button Grid.Column="3" Content="M-" Command="{Binding MemorySubtractCommand}" Style="{StaticResource Win11MemoryButtonStyle}"/>
            <Button Grid.Column="4" Content="MS" Command="{Binding MemoryStoreCommand}" Style="{StaticResource Win11MemoryButtonStyle}"/>
        </Grid>

        <!-- 3. Keypad Grid (6 Rows x 4 Columns) -->
        <Grid Grid.Row="2" FlowDirection="LeftToRight">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Row 0: %, CE, C, ⌫ -->
            <Button Grid.Row="0" Grid.Column="0" Content="%" Command="{Binding CalculatePercentCommand}" Style="{StaticResource Win11OpButtonStyle}"/>
            <Button Grid.Row="0" Grid.Column="1" Content="CE" Command="{Binding ClearEntryCommand}" Style="{StaticResource Win11OpButtonStyle}"/>
            <Button Grid.Row="0" Grid.Column="2" Content="C" Command="{Binding ClearAllCommand}" Style="{StaticResource Win11OpButtonStyle}"/>
            <Button Grid.Row="0" Grid.Column="3" Content="⌫" Command="{Binding BackspaceCommand}" Style="{StaticResource Win11OpButtonStyle}"/>

            <!-- Row 1: 1/x, x², √x, ÷ -->
            <Button Grid.Row="1" Grid.Column="0" Content="¹/x" Command="{Binding CalculateReciprocalCommand}" Style="{StaticResource Win11OpButtonStyle}"/>
            <Button Grid.Row="1" Grid.Column="1" Content="x²" Command="{Binding CalculateSquareCommand}" Style="{StaticResource Win11OpButtonStyle}"/>
            <Button Grid.Row="1" Grid.Column="2" Content="√x" Command="{Binding CalculateSquareRootCommand}" Style="{StaticResource Win11OpButtonStyle}"/>
            <Button Grid.Row="1" Grid.Column="3" Content="÷" Command="{Binding SetOperatorCommand}" CommandParameter="÷" Style="{StaticResource Win11OpButtonStyle}"/>

            <!-- Row 2: 7, 8, 9, × -->
            <Button Grid.Row="2" Grid.Column="0" Content="7" Command="{Binding InputDigitCommand}" CommandParameter="7" Style="{StaticResource Win11CalcButtonStyle}"/>
            <Button Grid.Row="2" Grid.Column="1" Content="8" Command="{Binding InputDigitCommand}" CommandParameter="8" Style="{StaticResource Win11CalcButtonStyle}"/>
            <Button Grid.Row="2" Grid.Column="2" Content="9" Command="{Binding InputDigitCommand}" CommandParameter="9" Style="{StaticResource Win11CalcButtonStyle}"/>
            <Button Grid.Row="2" Grid.Column="3" Content="×" Command="{Binding SetOperatorCommand}" CommandParameter="×" Style="{StaticResource Win11OpButtonStyle}"/>

            <!-- Row 3: 4, 5, 6, - -->
            <Button Grid.Row="3" Grid.Column="0" Content="4" Command="{Binding InputDigitCommand}" CommandParameter="4" Style="{StaticResource Win11CalcButtonStyle}"/>
            <Button Grid.Row="3" Grid.Column="1" Content="5" Command="{Binding InputDigitCommand}" CommandParameter="5" Style="{StaticResource Win11CalcButtonStyle}"/>
            <Button Grid.Row="3" Grid.Column="2" Content="6" Command="{Binding InputDigitCommand}" CommandParameter="6" Style="{StaticResource Win11CalcButtonStyle}"/>
            <Button Grid.Row="3" Grid.Column="3" Content="-" Command="{Binding SetOperatorCommand}" CommandParameter="-" Style="{StaticResource Win11OpButtonStyle}"/>

            <!-- Row 4: 1, 2, 3, + -->
            <Button Grid.Row="4" Grid.Column="0" Content="1" Command="{Binding InputDigitCommand}" CommandParameter="1" Style="{StaticResource Win11CalcButtonStyle}"/>
            <Button Grid.Row="4" Grid.Column="1" Content="2" Command="{Binding InputDigitCommand}" CommandParameter="2" Style="{StaticResource Win11CalcButtonStyle}"/>
            <Button Grid.Row="4" Grid.Column="2" Content="3" Command="{Binding InputDigitCommand}" CommandParameter="3" Style="{StaticResource Win11CalcButtonStyle}"/>
            <Button Grid.Row="4" Grid.Column="3" Content="+" Command="{Binding SetOperatorCommand}" CommandParameter="+" Style="{StaticResource Win11OpButtonStyle}"/>

            <!-- Row 5: ±, 0, ., = -->
            <Button Grid.Row="5" Grid.Column="0" Content="±" Command="{Binding ToggleNegateCommand}" Style="{StaticResource Win11CalcButtonStyle}"/>
            <Button Grid.Row="5" Grid.Column="1" Content="0" Command="{Binding InputDigitCommand}" CommandParameter="0" Style="{StaticResource Win11CalcButtonStyle}"/>
            <Button Grid.Row="5" Grid.Column="2" Content="." Command="{Binding InputDigitCommand}" CommandParameter="." Style="{StaticResource Win11CalcButtonStyle}"/>
            <Button Grid.Row="5" Grid.Column="3" Content="=" Command="{Binding CalculateEqualsCommand}" Style="{StaticResource Win11EqualButtonStyle}"/>
        </Grid>
    </Grid>
</UserControl>
